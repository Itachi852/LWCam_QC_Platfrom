# Alembic Migrations

This rewrite initially targets the existing LWCam PostgreSQL schema, so no new migration is required for the first compatible version.

When this backend is extracted as an independent project, add Alembic revisions here.

